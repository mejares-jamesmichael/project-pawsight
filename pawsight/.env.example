# PawSight Environment Variables
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!

# AI Chat Service Configuration
# The webhook URL for the AI chat backend service
CHAT_API_URL=https://your-backend-service.com/webhook/your-endpoint

# JWT Authentication Secret (64-byte base64-encoded key)
# Generate a secure random key and use the same secret in your backend
# Example generation (PowerShell):
#   [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
# Example generation (Linux/Mac):
#   openssl rand -base64 64
JWT_SECRET=your-64-byte-base64-encoded-secret-key-here

# TheCatAPI Configuration
# Get your free API key at: https://thecatapi.com/
# Provides access to cat images, breed information, and more
THE_CAT_API_KEY=your-thecatapi-key-here
